# nls
ls without month abbreviation.
## Overview
`ls -l` 実行時に表示される英語の月の略称を、数字に変更したコマンド。
- `ls -l` の実行結果にはファイルの変更日時が表示されるが、月の表記が英語であるため分かりにくい。
- `nls` は `-l` を使用した場合に、変更日時を `月 日 時刻` で表示する。
    - 変更日時が実行時刻の前年度以前である場合、 `月 日 (年)` で表示する。
